# Example only — never commit real tokens.
# Hermes Bot stores URL + token in expo-secure-store after first-run connect.
# There is no required .env for local Expo start.

# HERMES_BASE_URL=http://192.168.1.10:9119
# HERMES_AUTH_TOKEN=
